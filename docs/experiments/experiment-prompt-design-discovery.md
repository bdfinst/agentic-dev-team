# Design-Discovery Experiment — merged

> **Retired / merged.** This experiment is now **Axis B (workflow + refactor
> discipline)** of the unified study:
> [`experiment-prompt-when-tdd-pays.md`](experiment-prompt-when-tdd-pays.md).
>
> The merged prompt keeps this experiment's contributions: the four autonomous arms
> (`tdd-refactor` / `tdd-no-refactor` / `test-after` / `bduf`), the **open-design
> tasks with a trap**, the withheld **change chain** measuring **changeability**
> (blast radius, public-API churn, regression catch), deterministic **radon**
> structural metrics, the blind **multi-rater** design score, and the H-B2
> refactor-not-test-order isolation. Use that file.
