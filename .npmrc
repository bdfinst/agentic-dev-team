# Enforce the engines floor (package.json -> engines.node ">=24"). Without this,
# npm only warns on an old Node; engine-strict makes `npm ci`/`npm install` fail,
# so every environment (local, CI, cloud) actually runs at least Node 24.
engine-strict=true
