# Feature: to_roman

Implement `to_roman(n)` in `roman.py`.

- Converts an integer `1..3999` to its standard Roman numeral string
  (e.g. 4 -> "IV", 1994 -> "MCMXCIV").
- Raises `ValueError` for `n` outside `1..3999`.
