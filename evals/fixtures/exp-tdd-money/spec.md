# Feature: money (money.py)
Work in integer cents. Implement in `money.py`:
- `parse_money(s)` — parse "$1,234.56" into an integer number of cents (123456).
- `format_money(cents)` — inverse: "$1,234.56" with thousands separators and two
  decimals. e.g. format_money(0) == "$0.00".
