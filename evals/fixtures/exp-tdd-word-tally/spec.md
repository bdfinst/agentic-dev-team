# Feature: word_count

Implement `word_count(text)` in `tally.py`.

- Returns a dict mapping each word to the number of times it occurs.
- A "word" is a maximal run of ASCII letters. Everything else (spaces,
  punctuation, digits) separates words.
- Case-insensitive: keys are lowercased.
- `word_count("")` returns `{}`.
