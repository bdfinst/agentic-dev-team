# Feature: matrices (matrix.py)
Implement in `matrix.py` (matrices are lists of equal-length rows):
- `transpose(m)` — swap rows/columns.
- `add(a, b)` — element-wise sum of two same-shape matrices.
- `identity(n)` — n x n identity matrix.
