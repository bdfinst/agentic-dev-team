# Feature: encrypt (Caesar cipher)

Implement `encrypt(text, shift)` in `caesar.py`: shift each ASCII letter forward
by `shift` (wrapping z->a), preserving case; non-letters unchanged.
`encrypt("abc", 1) == "bcd"`, `encrypt("xyz", 3) == "abc"`.
