# Feature: encode (run-length)

Implement `encode(s)` in `rle.py`: run-length encode a string so each maximal
run of one character becomes the character followed by its count.
`encode("aaabbc") == "a3b2c1"`, `encode("") == ""`.
