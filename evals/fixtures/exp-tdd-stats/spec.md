# Feature: descriptive statistics (stats.py)
Implement, in `stats.py`, over a non-empty list of numbers:
- `mean(xs)` — arithmetic mean.
- `median(xs)` — middle value (average of the two middle values for even length).
- `variance(xs)` — population variance (divide by N).
