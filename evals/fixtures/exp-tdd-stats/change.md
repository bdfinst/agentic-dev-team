# Change: add mode and data_range
Add to `stats.py`:
- `mode(xs)` — most frequent value; on a tie return the smallest such value.
- `data_range(xs)` — max minus min.
Existing functions must not change.
