# Feature: evaluate (RPN)

Implement `evaluate(expr)` in `rpn.py`. `expr` is a space-separated Reverse
Polish Notation expression over `+ - * /` and numbers. Returns the numeric
result. e.g. `evaluate("3 4 +") == 7`.
