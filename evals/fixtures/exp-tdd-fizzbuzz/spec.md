# Feature: fizzbuzz

Implement `fizzbuzz(n)` in `fizzbuzz.py`. Returns a list of `n` strings for the
numbers `1..n`:
- multiples of 3 -> "Fizz", multiples of 5 -> "Buzz",
- multiples of both -> "FizzBuzz", otherwise the number as a string.
