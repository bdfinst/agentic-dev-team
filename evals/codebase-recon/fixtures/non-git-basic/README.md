# non-git-basic fixture

A minimal tree exercising filesystem-walk enumeration: a handful of kept
files plus one representative of every prefix- and filename-exclude in
`plugins/dev-team/knowledge/recon-inventory-excludes.txt`.

Used by `evals/codebase-recon/tests/inventory-non-git.sh`.
