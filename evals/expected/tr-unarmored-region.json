{
  "fixture": "tr-unarmored-region",
  "description": "Distinguishes an unarmored region (parseConfig — no tests, no defensive attention) from ordinary coverage gaps (validateEmail, formatDisplayName — some tests exist, edge cases missing). Expects the unarmored region named distinctly from coverage gaps.",
  "applicableAgents": ["test-review"],
  "agents": {
    "test-review": {
      "expectedStatus": "fail",
      "issueCount": { "min": 2, "max": 6 },
      "severities": {
        "warning": { "min": 1, "max": 3 }
      },
      "mustMention": ["unarmored", "parseConfig"]
    }
  }
}
