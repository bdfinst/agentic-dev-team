# cart

A shopping cart checkout calculator. Build the feature described in `spec.md`.

Production code goes in `cart.py`. Write tests as pytest tests in files named
`test_*.py` so they run with `python -m pytest -q`.
